import { describe, it, expect } from '@jest/globals';

describe('transactions (e2e)', () => {
  it('creates a transaction and computes total', async () => {
    // TODO: spin up test DB and run migration, then call POST /transactions
    expect(true).toBe(true);
  });
});
