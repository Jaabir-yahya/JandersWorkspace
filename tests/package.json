{
  "name": "@project-bridge/tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:integration": "jest --runInBand --detectOpenHandles",
    "test:integration:watch": "jest --runInBand --watch"
  },
  "devDependencies": {
    "@nestjs/testing": "^11.0.0",
    "@types/jest": "^29.5.0",
    "@types/supertest": "^6.0.0",
    "jest": "^29.5.0",
    "supertest": "^7.0.0",
    "ts-jest": "^29.0.0"
  },
  "dependencies": {
    "@project-bridge/api": "file:../apps/api"
  }
}
